library(dashBootstrapComponents)

row <- dbcRow(
  list(
    dbcCol(div("One of three columns")),
    dbcCol(div("One of three columns")),
    dbcCol(div("One of three columns"))
  ),
  className = "g-0"
)
