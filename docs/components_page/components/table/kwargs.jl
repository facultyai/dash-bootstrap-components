table = dbc_table(
    # using the same table as in the above example
    table_header * table_body,
    bordered = true,
    dark = true,
    hover = true,
    responsive = true,
    striped = true,
);
